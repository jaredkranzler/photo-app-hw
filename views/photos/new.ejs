<!DOCTYPE html>
<html>
<head>
  <title></title>
</head>
<body>
  <header>
    <h1>Add a Photo</h1>
    <nav>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/photos">Photo index</a></li>
      </ul>
    </nav>
  </header>
  <main>
    <form action="/photos" method="POST">
      <select name="userId">
        <% for (let i = 0; i < users.length; i++) { %>
          <option value="<%= users[i].id%>"><%=users[i].username%></option>
        <% } %>
      </select><br>

      <input type="text" name="title" placeholder="title" /><br>
      <input type="text" name="url" src="photo.url" placeholder="url" />
      <textarea name="body" placeholder="tell us about your photo!"></textarea><br>
      <input type="submit" value="Publish Photo"/>
    </form>
  </main>
</body>
</html>